# Next.js here

This repo uses a current Next.js generation; APIs and file layout can differ from older tutorials. Before changing App Router code, skim the guides under `node_modules/next/dist/docs/` and watch for deprecations.
